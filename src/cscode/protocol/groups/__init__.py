"""Protocol group definitions — API endpoint contracts organized by domain.

Each module in this package defines the typed contracts for a group of
related API endpoints. The contracts use TypedDict and dataclass types
from the schema layer — no FastAPI dependency.
"""
